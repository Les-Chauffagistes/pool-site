[extend]
useDefault = true

[[rules]]
id = "url-embedded-credentials"
description = "URL avec identifiants Basic Auth en clair (scheme://user:password@host)"
regex = '''[a-zA-Z][a-zA-Z0-9+.\-]{2,}://[^\s'"/:@$]{1,64}:([^\s'"/:@$]{8,255})@[a-zA-Z0-9.\-]+'''
entropy = 3.3
keywords = ["://"]
